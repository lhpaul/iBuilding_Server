= form_for @device_user do |f|
  - if @device_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@device_user.errors.count, "error")} prohibited this device_user from being saved:"
      %ul
        - @device_user.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
