
- if user_signed_in?
  %h3 Rooms
  %table.table.table-hover
    %thead
      %tr
        %th Name
        %th n dispositivos  
    %tbody
      -if Room.count > 0
        - Room.all.each do |room|
          %tr
            %td 
              = link_to room.name, room
            %td
              = room.devices.count
      -else
        %div.alert.alert-error No existen piezas


- else  
  %h2 Bienvenido a iBuilding!
  %a.btn.btn-large{ :href => "users/sign_in"}
    Login
  