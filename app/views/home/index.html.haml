
- if user_signed_in?
  %h3 Devices
  %table.table.table-hover
    %thead
      %tr
        %th Name
        %th Ip     
    %tbody
      -if Device.count > 0
        - Device.all.each do |device|
          %tr
            %td 
              = link_to device.name, device
            %td
              = device.ip
      -else
        %div.alert.alert-error No existen dispositivos
  %h3 Data
  %table.table.table-hover
    %thead
      %tr
        %th Fecha
        %th Id
        %th Tipo
        %th Data
    %tbody
      - Datum.desc(:date).limit(100).each do |datum|
        %tr
          %td 
            = datum.date
          %td
            = datum.id_dispositivo
          %td
            = datum.tipo
          %td
            = datum.data        

  %h3 Request
  %table.table.table-hover
    %thead
      %tr
        %th Ip
        %th Date
    %tbody
      - Request.desc(:date).limit(100).each do |req|
        %tr
          %td 
            = req.ip
          %td 
            = req.date


- else  
  %h2 Bienvenido a iBuilding!
  %a.btn.btn-large{ :href => "users/sign_in"}
    Login
  